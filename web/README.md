# web

Next.js public explorer — registry stats, search, agent profiles, and the published
scoring methodology. Deployed to Vercel (see [`infra/README.md`](../infra/README.md)).

Built in **Group 8** (explorer). Reads the public API at `NEXT_PUBLIC_API_BASE_URL`.

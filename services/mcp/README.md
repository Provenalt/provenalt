# mcp

MCP server exposing `check_provenalt(agentId)` and `check_eligibility(wallet, token)` — a
thin client of the `api` service, installable via npx.

Built in **Group 9** (monetized distribution).
